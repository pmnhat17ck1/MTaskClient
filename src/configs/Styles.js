import Colors from './Colors';

// eslint-disable-next-line import/prefer-default-export
export const GrayUnderlineStyle = {
  border: `1px solid ${Colors.Gray21}`,
  borderRightWidth: 0,
  borderLeftWidth: 0,
  borderTopWidth: 0,
};
